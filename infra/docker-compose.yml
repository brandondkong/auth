services:
  db:
    image: 'postgres:18.3-alpine'

    ports:
      - 5432:5432
    
    env_file:
      - .env

    environment:
      POSTGRES_DB: auth
    
    volumes:
      - /var/lib/postgresql/data/
